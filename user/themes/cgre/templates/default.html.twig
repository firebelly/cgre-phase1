{% extends 'partials/base.html.twig' %}

{% block content %}
		<div class="container">
			<h1 class="divot">{{ header.display_title }}</h1>

			<div class="body-wrap user-content">
				{{ page.content|raw }}
			</div>
		</div>
{% endblock %}
