{% extends 'partials/base.html.twig' %}

{% block content %}
		<div class="container">
			<h1 class="divot">{{ page.title }}</h1>

			<div class="body-wrap user-content">
				{{ page.content }}
			</div>
		</div>
{% endblock %}
